greet('Sven'); //Zahlen (number) gehen nicht nur Strings (wörter)
/**
 * 
 * @param name - this is the name at string
 * @returns hello + name
 */
async function greet(name: string){
    return 'Hello ' + name;
}